import { supabase } from '../supabase';

/**
 * Fonctions d'administration pour Supabase
 * Note: La logique complexe sera ajoutée dans les étapes suivantes.
 */

export const getAdminStats = async () => {
    // Skeleton function
    return null;
};

export const getRecentOrders = async () => {
    // Skeleton function
    return [];
};
